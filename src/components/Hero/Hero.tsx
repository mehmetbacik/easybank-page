import React from 'react';
import styles from './styles/Hero.module.scss';

const Hero: React.FC = () => {
  return (
    <header className={styles.hero}>
      Hero
    </header>
  );
};

export default Hero;