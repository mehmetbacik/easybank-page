import React from 'react';
import styles from './styles/Hero.module.scss';

const Hero: React.FC = () => {
  return (
    <div className={styles.hero}>
      Hero
    </div>
  );
};

export default Hero;